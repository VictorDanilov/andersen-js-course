﻿/**
 * TODO: описание
 *
 * Пример вызова
 * console.log(task12Old({isBestField: 2}, 'isBestField')); -> 2
 * console.log(task12Old({isBestField: 2}, 'isBestField2')); -> undefined
 */

export function task12Old(myBestObject, myFieldName) {
  return myBestObject && myBestObject[myFieldName];
}

// Напишите реализацию функции task2Old на ESnext ниже этого комментария.
// При желании, можете использовать стрелочную функцию, вместо обычной

export function task12New() {}
